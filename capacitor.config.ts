import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.personal.app',
  appName: 'spaceAvailable',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
